syntax = "proto3";
package file_transfer;

service FileTransfer {
  rpc RequestFile (FileRequest) returns (FileReply);
  rpc DefaultFile (NoFileRequest) returns (FileReply);
}

message NoFileRequest {
}

message FileRequest {
  string filename = 1;
}

message FileReply {
  bytes data = 1;
}
